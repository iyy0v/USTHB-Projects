#include <stdio.h>

main()
{
    int x,y,N;
    printf(" Please, enter N = ");
    scanf("%d",&N);
    for(x=0;x<=N;x++) {
                        for(y=0;y<=N;y++){printf("( %d , %d )\n",x,y);

    }
    }
}
